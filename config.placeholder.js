/**
 * =============================================
 *  CONFIG.JS — Placeholder / Dummy
 * =============================================
 *  File ini adalah PLACEHOLDER yang aman untuk di-push ke GitHub.
 *  TIDAK berisi secret apapun.
 *
 *  Untuk penggunaan lokal/production:
 *  1. Salin file ini menjadi "config.js"
 *  2. Isi ADMIN_SECRET dengan kunci rahasia kamu
 *  3. config.js sudah di-gitignore, jadi tidak akan ter-upload
 *
 *  gallery.js sudah menangani kasus config.js tidak ada:
 *  (typeof CONFIG !== 'undefined' && CONFIG.ADMIN_SECRET)
 *    ? CONFIG.ADMIN_SECRET : ''
 * =============================================
 */
const CONFIG = {
    ADMIN_SECRET: '' // ← ISI DENGAN SECRET KAMU DI FILE config.js LOKAL
};
